import { Router } from 'express'

const login_rota = Router()

// eslint-disable-next-line @typescript-eslint/no-unused-vars
login_rota.post('/', async (_req, _res) => {
  // await fazerLogin(req, res)
})

export default login_rota
